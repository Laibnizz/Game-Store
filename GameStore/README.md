# Game Store

---

## 📁 專案資料夾結構

```text
GameStore/
├── server/
│   ├── app.py                 # Lobby + Game Store 主伺服器
│   ├── database.json          # 使用者與遊戲資料庫
│   └── uploaded_games/        # 開發者上傳的遊戲檔案
│
├── client_player/
│   ├── app.py                 # 玩家 Client
│   └── downloads/
│       └── <username>/        # 玩家下載的遊戲存放位置
│
├── client_dev/
|   ├── games/                 # 開發者存在遊戲的位置
│   └── app.py                 # 開發者 Client
│
├── templates/
│   └── game_template.py       # 遊戲模板（供開發者使用）
│
├── database.json              # 資料儲存
└── README.md                  # 本說明文件
```

---

## 執行環境需求

- Python **3.9 以上**
- Windows / Linux / macOS
- 僅使用 Python 標準函式庫，無需額外套件

---

## 啟動方式

### 啟動 Lobby Server
注意: 
連線資訊（IP / Port）in dev/player app.py

```python
SERVER_HOST = "140.113.17.11" #linux1
SERVER_PORT = 12088
```

在專案根目錄執行：

```bash
python server/app.py
```

預設設定：

- IP：`0.0.0.0`
- Port：`12088`

---

### 啟動 Developer Client（開發者端）

```bash
python client_dev/app.py
```

功能：

- 開發者登入 / 註冊
- 上傳遊戲檔案
- 管理已上傳遊戲

---

### 啟動 Player Client（玩家端）

```bash
python client_player/app.py
```

功能：

- 玩家登入 / 註冊
- 瀏覽遊戲清單
- 下載並遊玩遊戲

---


## 遊戲上傳與下載說明

- 上傳 / 下載時，Server 會動態開啟一個 **一次性 TCP Port**
- Client 連線後直接傳送 **raw bytes**
- 上傳後的遊戲檔案會存放於：

```text
server/uploaded_games/
```

- 玩家下載後的遊戲檔案會存放於：

```text
client_player/downloads/<username>/
```

---

## 遊戲開發說明

- 請以 `templates/game_template.py` 為基礎複製並修改

---